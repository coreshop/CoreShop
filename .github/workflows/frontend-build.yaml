name: CoreShop Studio Frontend Build

# Rebuild trigger: regenerate Studio bundles via PR to satisfy branch ruleset.
on:
  pull_request_target:
    branches: [ '5.1', '2026.x' ]
  workflow_dispatch:

permissions:
  contents: read

# dummy change to trigger frontend build

jobs:
  build:
    permissions:
      contents: write
    uses: ./.github/workflows/shared-frontend-build.yaml
