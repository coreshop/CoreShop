{%- macro format(amount, currency_code) -%}
    {{ amount|coreshop_format_money(currency_code, 'de') }}
{%- endmacro -%}

{%- macro convertAndFormat(amount) -%}
    {% from _self import format %}

    {{- format(amount|coreshop_convert_money(coreshop.store.baseCurrency.isoCode, coreshop.currencyCode), coreshop.currencyCode) }}
{%- endmacro -%}
