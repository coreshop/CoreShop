/**
 * CoreShop Studio RSBuild Wrapper Configuration
 * 
 * This wrapper uses the template config for the studio-build.js system.
 * It expects the bundle to be specified via CORESHOP_BUNDLE_NAME env var.
 */

// Re-export the template configuration
export { default } from './rsbuild.template.config'