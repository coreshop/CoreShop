coreshop_admin_carrier:
    type: coreshop.resources
    resource: |
      alias: coreshop.carrier
      additional_routes:
        config:
          path: get-config
          action: getConfig
          methods:
            - GET

coreshop_admin_shipping_rule:
    type: coreshop.resources
    resource: |
      alias: coreshop.shipping_rule
      additional_routes:
        config:
          path: get-config
          action: getConfig
          methods:
            - GET
