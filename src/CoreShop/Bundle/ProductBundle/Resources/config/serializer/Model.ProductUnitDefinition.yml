CoreShop\Component\Product\Model\ProductUnitDefinition:
    exclusion_policy: ALL
    xml_root_name: product_unit_definition
    properties:
        id:
            expose: true
            type: integer
            xml_attribute: true
            groups: [List, Detailed, Version]
        unit:
            expose: true
            type: CoreShop\Component\Product\Model\ProductUnit
            groups: [List, Detailed]
        conversionRate:
            expose: true
            type: float
            groups: [List, Detailed, Version]
        precision:
            expose: true
            type: int
            groups: [List, Detailed, Version]
        productUnitDefinitions:
            expose: true
            type: CoreShop\Component\Product\Model\ProductUnitDefinitions
            groups: [List, Detailed]
    virtual_properties:
        unit:
            exp: 'object.getUnit() ? object.getUnit().getId() : null'
            type: integer
            serialized_name: unit
            groups: [Version]
