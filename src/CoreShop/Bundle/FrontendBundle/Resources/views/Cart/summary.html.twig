{% extends '@CoreShopFrontend/layout.html.twig' %}
{% form_theme form '@CoreShopFrontend/Form/theme.html.twig' %}

{% block content %}

    {% include '@CoreShopFrontend/Checkout/_order-steps.html.twig' %}
    {% include '@CoreShopFrontend/Common/flash_messages.html.twig' %}

    {% if cart.hasItems() %}
        <div class="cart-summary">
            {% include '@CoreShopFrontend/Cart/Summary/_items.html.twig' %}
        </div>
    {% else %}
        <p>{{ 'coreshop.ui.cart_empty'|trans }}</p>
    {% endif %}

    <div class="row">
        <div class="col-xs-12 col-sm-2">
            <a href="{{ path('coreshop_index') }}" class="btn btn-default w-100">
                {{ 'coreshop.ui.continue_shopping'|trans }}
            </a>
        </div>
        {% if cart.hasItems() %}
            <div class="col-xs-12 col-sm-1 col-sm-push-9">
                <a href="{{ path('coreshop_checkout', {'stepIdentifier': coreshop_checkout_steps_get_first()}) }}" class="btn btn-main w-100">
                    {{ 'coreshop.ui.checkout'|trans }}
                </a>
            </div>
        {% endif %}
    </div>

{% endblock %}