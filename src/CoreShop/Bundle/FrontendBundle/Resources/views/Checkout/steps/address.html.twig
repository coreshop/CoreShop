{% extends '@CoreShopFrontend/Checkout/layout.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block checkout_title %}
    {{ 'coreshop.ui.address'|trans }}
{% endblock %}

{% block checkout %}
    <div class="panel">
        <div class="panel-body">
            {% if form.invoiceAddress.vars.choices|length > 0 or form.shippingAddress.vars.choices|length > 0 %}
                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

                {{ form_errors(form) }}

                <div class="row">
                    {% if hasShippableItems %}
                        <div class="col-xs-12 col-sm-6">
                            {{ form_row(form.shippingAddress) }}
                        </div>
                    {% endif %}

                    <div class="col-xs-6 invoice-address-selector" style="{% if hasShippableItems == true and form.useShippingAsInvoice.vars.data %} display:none {% endif %}">
                        {{ form_row(form.invoiceAddress) }}
                    </div>
                </div>

                {% if hasShippableItems %}
                    <div class="row">
                        <div class="col-xs-12">
                            {{ form_row(form.useShippingAsInvoice) }}
                        </div>
                    </div>
                {% endif %}

                <div class="row">

                    {% if hasShippableItems %}
                        <div class="col-xs-12 col-sm-6">
                            <div class="panel panel-smart">
                                <div class="panel-heading">
                                    <h4 class="panel-title">
                                        {{ 'coreshop.ui.shipping_address'|trans }}
                                    </h4>
                                </div>
                                <div class="panel-body panel-shipping-address">

                                </div>
                            </div>
                        </div>
                    {% endif %}

                    <div class="col-xs-12 col-sm-6">
                        <div class="panel panel-smart">
                            <div class="panel-heading">
                                <h4 class="panel-title">
                                    {{ 'coreshop.ui.invoice_address'|trans }}
                                </h4>
                            </div>
                            <div class="panel-body panel-invoice-address">

                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-xs-12">
                        <a href="{{ path('coreshop_customer_address_new', {'_redirect': path('coreshop_checkout', {stepIdentifier: identifier})}) }}" class="btn btn-default pull-left">
                            {{ 'coreshop.ui.add_new_address'|trans }}
                        </a>
                    </div>
                </div>
                <br>
                 <div class="row">
                    {% set previousStep = coreshop_checkout_steps_get_previous() %}
                    {% if previousStep is not null %}
                        <div class="col-xs-12 col-sm-6">
                            <a href="{{ path('coreshop_checkout', {'stepIdentifier': previousStep}) }}" class="btn">
                                {{ 'coreshop.ui.back'|trans }}
                            </a>
                        </div>
                    {% endif %}
                    <div class="col-xs-12 {{ previousStep is not null ? 'col-sm-6' : '' }}">
                        <button type="submit" class="btn btn-default pull-right">
                            {{ 'coreshop.ui.proceed'|trans }}
                        </button>
                    </div>
                </div>

                {{ form_end(form) }}

            {% else %}
                <p>{{ 'coreshop.ui.no_address_found'|trans }}</p>

                <div class="row">
                    <div class="col-xs-12">
                        <a href="{{ path('coreshop_customer_address_new', {'_redirect': path('coreshop_checkout', {stepIdentifier: identifier})}) }}" class="btn btn-default pull-left">
                            {{ 'coreshop.ui.add_new_address'|trans }}
                        </a>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>

{% endblock %}
