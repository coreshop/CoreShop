{% if objects|length > 0 %}
<div class="list-group">
    {% if label %}
        <div class="list-group-item">
            {{ label|trans }}
        </div>
    {% endif %}

    <div class="list-group-item">
        <div class="filter-group filter-group-checkbox">
            {% for value in objects %}
                {% if value %}
                    <label class="radio">
                        <input name="{{ fieldName }}" type="radio" value="{{ value.id }}" {% if value.id == currentValues %} checked="checked" {% endif %}>
                        {{ value.name }}
                    </label>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</div>
{% endif %}
