{% set method = isGoogleTagMode == true ? 'gtag' : 'ga' %}
{% for key, call in calls %}
     {{ method }}('ec:addProduct', {{ call|json_encode()|raw }});
{% endfor %}

{{ method }}('ec:setAction', 'checkout', {{ actionData|json_encode()|raw }});
{{ method }}('send', 'event', 'Checkout', 'Step');