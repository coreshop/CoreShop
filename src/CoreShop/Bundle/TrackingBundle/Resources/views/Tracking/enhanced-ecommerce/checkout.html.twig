{% if products %}
    {% for prItem in products %}
        ga('ec:addProduct', {{ prItem|json_encode }});
    {% endfor %}
{% endif %}
ga('ec:setAction', 'checkout', {{ actionData|json_encode() }});
ga('send','event','Checkout','Step');
